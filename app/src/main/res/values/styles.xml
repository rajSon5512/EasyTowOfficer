<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="EntryButtonStyle">
        <item name="android:layout_width">@dimen/entry_button_size</item>
        <item name="android:layout_height">@dimen/entry_button_size</item>
        <item name="android:background">@android:color/holo_blue_bright</item>
    </style>

    <style name="bigfontsize">

        <item name="android:layout_width">@dimen/big_font_size</item>

    </style>


</resources>
